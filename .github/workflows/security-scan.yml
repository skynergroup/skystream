name: 🔒 Security Scan

on:
  push:
    branches: [ main, production, develop ]
  pull_request:
    branches: [ main, production, develop ]
  schedule:
    # Run security scan every Monday at 9 AM
    - cron: '0 9 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  # Static Analysis with ESLint Security Plugin (Free)
  eslint-security:
    name: 🔍 ESLint Security Analysis
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Install ESLint security plugin
      run: npm install --no-save eslint-plugin-security

    - name: Run security linting
      run: |
        echo "Running security-focused ESLint checks..."
        npx eslint "src/**/*.{js,jsx}" \
          --config '{"extends": ["plugin:security/recommended"], "plugins": ["security"], "env": {"browser": true, "es6": true}, "parserOptions": {"ecmaVersion": 2020, "sourceType": "module"}}' \
          --format json > eslint-security.json || true

        # Check results and display them
        if [ -s eslint-security.json ]; then
          echo "📊 Security linting results:"
          cat eslint-security.json | jq -r '.[] | select(.messages | length > 0) | "\(.filePath): \(.messages | length) issues"' || echo "Results found but couldn't parse with jq"

          # Count critical security issues
          CRITICAL_COUNT=$(cat eslint-security.json | jq '[.[] | .messages[] | select(.severity == 2)] | length' 2>/dev/null || echo "0")
          echo "Critical security issues found: $CRITICAL_COUNT"

          if [ "$CRITICAL_COUNT" -gt 0 ]; then
            echo "⚠️ Critical security issues detected. Please review."
          else
            echo "✅ No critical security issues found"
          fi
        else
          echo "✅ No security issues found in linting"
        fi

    - name: Upload security linting results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: eslint-security-${{ github.sha }}
        path: eslint-security.json
        retention-days: 30

  # Dependency vulnerability scan
  dependency-scan:
    name: 📦 Dependency Scan
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run npm audit
      run: |
        echo "Running comprehensive npm audit..."
        npm audit --audit-level=low --json > audit-full.json || true
        npm audit --audit-level=moderate --json > audit-moderate.json || true
        
        # Check for high/critical vulnerabilities
        if npm audit --audit-level=high; then
          echo "✅ No high or critical vulnerabilities found"
        else
          echo "❌ High or critical vulnerabilities detected!"
          npm audit --audit-level=high
          exit 1
        fi

    - name: Upload audit results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: security-audit-${{ github.sha }}
        path: |
          audit-full.json
          audit-moderate.json
        retention-days: 30

  # Secret scanning
  secret-scan:
    name: 🔐 Secret Scan
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install TruffleHog
      run: |
        curl -sSfL https://raw.githubusercontent.com/trufflesecurity/trufflehog/main/scripts/install.sh | sh -s -- -b /usr/local/bin

    - name: Run TruffleHog scan
      run: |
        echo "Scanning for secrets..."
        trufflehog git file://. --json --no-update > secrets-scan.json || true
        
        # Check if any secrets were found
        if [ -s secrets-scan.json ]; then
          echo "⚠️ Potential secrets found!"
          cat secrets-scan.json | jq -r '.SourceMetadata.Data.Git.file' | sort | uniq
          echo "Please review the scan results and remove any exposed secrets"
        else
          echo "✅ No secrets detected"
        fi

    - name: Upload secret scan results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: secret-scan-${{ github.sha }}
        path: secrets-scan.json
        retention-days: 30

  # Additional security checks
  additional-security:
    name: 🛡️ Additional Security Checks
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Check for hardcoded secrets in code
      run: |
        echo "Checking for potential hardcoded secrets..."

        # Check for common secret patterns
        echo "Checking for API keys..."
        if grep -r "api[_-]key\s*=\s*['\"][^'\"]\{10,\}" src/ || true; then
          echo "⚠️ Potential API keys found in code"
        fi

        echo "Checking for passwords..."
        if grep -r "password\s*=\s*['\"][^'\"]\{5,\}" src/ || true; then
          echo "⚠️ Potential passwords found in code"
        fi

        echo "Checking for tokens..."
        if grep -r "token\s*=\s*['\"][^'\"]\{10,\}" src/ || true; then
          echo "⚠️ Potential tokens found in code"
        fi

        echo "✅ Hardcoded secret check completed"

    - name: Check for console.log in production code
      run: |
        echo "Checking for console.log statements..."
        if find src/ -name "*.js" -o -name "*.jsx" | xargs grep -n "console\.log" || true; then
          echo "⚠️ console.log statements found - consider removing for production"
        else
          echo "✅ No console.log statements found"
        fi

    - name: Check for eval() usage
      run: |
        echo "Checking for dangerous eval() usage..."
        if find src/ -name "*.js" -o -name "*.jsx" | xargs grep -n "eval(" || true; then
          echo "❌ eval() usage found - this is a security risk!"
        else
          echo "✅ No eval() usage found"
        fi



  # License compliance scan
  license-scan:
    name: 📄 License Scan
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Install license checker
      run: npm install -g license-checker

    - name: Check licenses
      run: |
        echo "Checking dependency licenses..."
        license-checker --json --out licenses.json
        
        # Check for copyleft licenses that might be problematic
        if license-checker --summary | grep -E "(GPL|AGPL|LGPL|CDDL|EPL|MPL)"; then
          echo "⚠️ Copyleft licenses detected. Please review for compliance."
          license-checker --summary
        else
          echo "✅ No problematic copyleft licenses found"
        fi

    - name: Upload license scan results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: license-scan-${{ github.sha }}
        path: licenses.json
        retention-days: 30

  # Security headers check
  security-headers:
    name: 🌐 Security Headers
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/production'
    steps:
    - name: Check security headers
      run: |
        echo "Checking security headers for production site..."
        
        # This would check your actual production site
        # Replace with your actual production URL
        PROD_URL="https://your-production-site.com"
        
        echo "Checking headers for: $PROD_URL"
        
        # Check for important security headers
        headers=$(curl -s -I "$PROD_URL" || echo "Site not accessible")
        
        echo "Response headers:"
        echo "$headers"
        
        # Check for specific security headers
        if echo "$headers" | grep -i "x-frame-options"; then
          echo "✅ X-Frame-Options header found"
        else
          echo "⚠️ X-Frame-Options header missing"
        fi
        
        if echo "$headers" | grep -i "x-content-type-options"; then
          echo "✅ X-Content-Type-Options header found"
        else
          echo "⚠️ X-Content-Type-Options header missing"
        fi
        
        if echo "$headers" | grep -i "strict-transport-security"; then
          echo "✅ HSTS header found"
        else
          echo "⚠️ HSTS header missing"
        fi

  # Security summary
  security-summary:
    name: 📋 Security Summary
    runs-on: ubuntu-latest
    needs: [eslint-security, dependency-scan, secret-scan, additional-security, license-scan]
    if: always()
    steps:
    - name: Generate security report
      run: |
        echo "## 🔒 Security Scan Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        if [[ "${{ needs.eslint-security.result }}" == "success" ]]; then
          echo "✅ **ESLint Security**: No critical issues found" >> $GITHUB_STEP_SUMMARY
        else
          echo "⚠️ **ESLint Security**: Issues detected" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [[ "${{ needs.dependency-scan.result }}" == "success" ]]; then
          echo "✅ **Dependency Scan**: No high/critical vulnerabilities" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **Dependency Scan**: Vulnerabilities found" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [[ "${{ needs.secret-scan.result }}" == "success" ]]; then
          echo "✅ **Secret Scan**: No secrets detected" >> $GITHUB_STEP_SUMMARY
        else
          echo "⚠️ **Secret Scan**: Potential secrets found" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [[ "${{ needs.additional-security.result }}" == "success" ]]; then
          echo "✅ **Additional Security**: No issues found" >> $GITHUB_STEP_SUMMARY
        else
          echo "⚠️ **Additional Security**: Issues found" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [[ "${{ needs.license-scan.result }}" == "success" ]]; then
          echo "✅ **License Scan**: Compliant" >> $GITHUB_STEP_SUMMARY
        else
          echo "⚠️ **License Scan**: Review required" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
        echo "**Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
        echo "**Triggered by**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
